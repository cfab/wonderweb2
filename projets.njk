---
layout: default
title: Dernières mise en ligne
icon: 🚀
eleventyNavigation:
  key: Réalisations
---
<div class="chapeau">
  Alors ici on est plutôt sur du site internet <em>local</em> et de <em>qualité</em>, fait avec passion en suivant
  l'évolution des technologies.
</div>
{% block contenu %}
<ol class="flex flex-col items-end mt-8 md:mt-16 content">
  {% for post in collections.projets | reverse %}
  <li class="flex flex-wrap w-full px-4 my-4 text-base md:my-8 md:px-0 md:w-3/4">
    <div class="hidden w-full md:block md:absolute md:-ml-24">
      <span class="date">{{ post.data.date | htmlDateString }}</span>
    </div>
    <div class="flex flex-1 md:px-4 ">
      <img src="{{ post.data.url }}/favicon.ico" class="inline w-6 h-6 mr-2 bg-uidark" alt="" width="16" height="16" />
      <div class="flex-1 md:ml-2">
        <a href="{{ post.data.url }}" target="_blank" rel="noopener"
          class="font-sans font-bold tracking-wider uppercase border-b-2 border-solid text-secondary border-secondary">{{ post.data.title }}</a>
        <div class="font-serif text-lg md:my-4 ">
          {{ post.data.description }}
        </div>
      </div>
    </div>
  </li>
  {% endfor %}
</ol>
{% endblock %}
